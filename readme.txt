1. Clone the repository:
   git clone <repository_url>
2. cd repository
3. composer install
4. cp .env.example .env
5. configure database in .env file
6. Run Migration :- Php artisan migrate
7. Run command :- php artisan coin:fetch after run thi command you can see data in coingecko database in coins table.